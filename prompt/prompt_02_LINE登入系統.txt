================================================================
公司任務中心 — 開發指令紀錄
指令編號：02
步驟：LINE 登入系統
建立日期：2026/05/26
================================================================

我要為「公司任務中心」建立 LINE Login 登入系統。

【專案資訊】
- Supabase Project URL：https://qvmhcglheoeudxzqrlyv.supabase.co
- LINE Channel ID：2010196855
- LINE Channel Secret：d9136ba5cc48f4e66b7680dc823c0cb6

【登入邏輯說明】
1. 使用者點「用 LINE 登入」按鈕
2. 跳到 LINE 授權頁面
3. 使用者同意後，LINE 回傳該使用者的 line_id、名字、大頭貼
4. 系統去 Supabase 的 users 表格比對這個 line_id
5. 找到 → 取得該使用者的 role（admin/boss/staff），進入系統
6. 找不到 → 顯示錯誤畫面「你沒有使用權限，請聯絡管理者」
7. 登入狀態存在瀏覽器，關掉重開不用重新登入

【三個角色的權限】
- admin（我）：可以讀寫刪除所有資料
- boss（老闆）：可以讀取大部分資料，只能對自己的待辦打勾
- staff（台中員工）：只能看展覽清點表、會議記錄，會議記錄可以新增

【請幫我完成以下工作】

1. 建立 React 專案（如果還沒有的話）
   - 使用 Vite + React
   - 安裝必要套件：supabase-js、react-router-dom

2. 建立環境變數檔案 .env
   - VITE_SUPABASE_URL
   - VITE_SUPABASE_ANON_KEY
   - VITE_LINE_CHANNEL_ID
   （不要把 Channel Secret 寫進前端程式，這個之後後端用）

3. 建立 LINE 登入流程
   - 登入頁：一個置中的「用 LINE 登入」按鈕
   - 點擊後導向 LINE 授權網址
   - 授權完成後，LINE 會把使用者導回 /auth/callback 這個頁面
   - callback 頁面處理：用 LINE 回傳的 code 換取使用者資料
   - 拿到 line_id 後去 Supabase users 表格比對
   - 比對成功 → 把使用者資料存到 localStorage → 導向首頁
   - 比對失敗 → 顯示無權限錯誤頁面

4. 建立簡單的首頁（暫時）
   - 顯示「登入成功，你好，[使用者名字]」
   - 顯示目前角色是什麼
   - 有一個登出按鈕

5. 建立路由保護
   - 未登入的人訪問任何頁面都跳回登入頁
   - 不同角色看到不同內容（這次先做基本架構，細部權限後面再加）

【注意事項】
- 改完請告訴我改了什麼，不要只丟 diff
- 不要動 Supabase 資料庫的表格結構
- 不要順便改其他地方
- LINE 的 Callback URL 要告訴我是什麼，我需要去 LINE Developers 後台設定

完成後請告訴我：
1. 建了哪些檔案
2. Callback URL 是什麼（我要去 LINE 後台填）
3. 接下來我要在 Supabase users 表格手動新增三個使用者的步驟

================================================================
備注：
- 這份指令對應建置順序第二步
- 完成後需要手動在 Supabase users 表格新增三筆使用者資料
- 完成後下一步是老闆待辦清單功能
- LINE Developers 後台：https://developers.line.biz/
- 本專案 Channel 名稱：公司任務中心
================================================================
